# HR Agent 招聘提效平台使用说明

> 适用版本：2026-08-20 阶段 7 React 完整 AI 初筛报告完成后的开发版
>
> 系统不附带演示数据。第一次启动后岗位、候选人、简历和 Application 列表为空，属于正常现象。

## 1. 启动项目

请先安装 Docker Desktop、Python 3.11、Node.js 18 或更高版本。在项目根目录双击：

```text
launch\start_project.bat
```

脚本会启动 PostgreSQL、Redis 和 Chroma，执行 Alembic 数据库迁移，然后启动后端与前端。默认页面是：

```text
http://localhost:5173/app/jobs
```

只检查环境：

```powershell
launch\start_project.bat -CheckOnly
```

启动但不自动打开浏览器：

```powershell
launch\start_project.bat -NoBrowser
```

后端 Swagger 接口文档位于 `http://localhost:8000/docs`。

## 2. 当前页面

| 页面 | 地址 | 当前用途 |
| --- | --- | --- |
| Dashboard | `/app/dashboard` | 查看真实岗位、候选人和待处理 Application 统计 |
| 简历管理 | `/app/resumes` | 查看已上传简历 |
| 候选人管理 | `/app/candidates` | 查看 HR 已通过的 Application 所对应候选人 |
| 新增候选人 | `/app/candidates/new` | HR 人工直通，或上传简历后识别并核对资料 |
| 岗位管理 | `/app/jobs` | 创建、编辑、开放、关闭、重新开放、安全删除岗位，并只读预览 JD 评价计划 |
| HR 初筛工作台 | `/app/screening` | 查看运行状态和当前 AI 报告，单人/批量重新评估，并独立执行 HR 决策 |
| 报告中心 | `/app/reports` | 只查看通用 Report；Application 的当前 AI 初筛报告在 `/app/screening` 查看 |
| 公开投递预览 | `/apply` | 查看开放岗位和页面外观，当前不能正式提交 |

当前没有登录和权限系统，仍是开发环境工作台。

## 3. 当前推荐流程

```text
创建并开放 Job
  -> 后端生成当前 JD 版本的只读 JobEvaluationPlan
  -> 上传 Resume、提取原文、可选 AI 结构化
  -> 创建或复用稳定 Candidate
  -> 创建绑定当前 Resume 的 Application
  -> PostgreSQL 保存 ScreeningRun，后台 worker 执行评价
  -> 成功事务替换唯一当前 ScreeningReport，失败保留旧报告
  -> HR 独立决定通过、备选或淘汰
  -> StageHistory 保存决定记录
```

旧 Rubric、五维权重、Python 确定性评分、推荐上限和多份 ScreeningResult 历史已经删除，不应再按旧流程准备评分标准或调用旧接口。

## 4. 创建并开放岗位

1. 打开 `/app/jobs`。
2. 新增岗位，填写岗位名称、部门、地点、职位描述和结构化要求。
3. 先保存草稿，检查完整后再开放。
4. 在岗位列表点击“评价计划”，只读检查当前状态、基础事项、来源、警告和版本。

草稿和关闭岗位不能接收新的申请；历史 Application 仍能显示其关闭岗位信息。岗位开放后，后端会按当前 JD 指纹生成只读评价计划。已有 Candidate、Resume、Application、JobEvaluationPlan 或 Report 引用的岗位不能直接删除。

## 5. 简历与候选人

在 `/app/candidates/new` 可以手工填写资料，也可以：

1. 上传 PDF、DOCX 或 TXT，单个文件最大 10 MB。
2. 后端把文件保存在私有目录。
3. 提取原文。
4. 调用 DeepSeek 生成结构化草稿。
5. HR 核对普通字段、教育、工作、项目和技能候选。
6. 确认后创建或复用 Candidate，并把 Resume 绑定到 Application。

AI 结构化草稿只是辅助填表，不是事实来源。联系方式、时间、公司、学校和经历在保存前都需要人工核对。

Candidate 表示“这个人”，Application 表示“这个人用哪份简历申请哪个岗位”。同一 Candidate 可以申请多个岗位，每个 Application 都独立绑定自己的当前 Resume，不能串用其他申请的简历。

## 6. Application 与 HR 决策

打开 `/app/screening`：

- “录入待审核申请”会先上传或选择已解析 Resume，再创建 Application。
- HR 可以直接通过、进入备选或淘汰，不依赖 AI 状态或 AI 分数。
- 改变已有决定时必须填写岗位相关理由。
- 淘汰后可以按明确理由撤销，回到人工审核。
- 每次决定写入 StageHistory，便于追溯。
- 年龄、性别、民族、婚育、籍贯、照片和学校名气等不能作为决定理由。
- 点击“查看 AI 报告”只读取辅助建议，不会自动提交任何 HR 决策。

HR 人工直通入口会在一个工作流中保存 Candidate、Resume 绑定、Application 和首次阶段历史。它不会自动调用已经删除的旧评分链路。

## 7. AI 初筛报告与重新评估

当前已经实现 JobEvaluationPlan、严格单次评价、PostgreSQL 持久运行、幂等复用、成功报告替换、失败保留、过期、React 完整报告和最多 20 人重新评估。除页面操作外，也可在 Swagger 中调用：

- `GET /api/v2/jobs/{job_id}/evaluation-plan`：只读查询当前计划；
- `POST /api/v2/jobs/{job_id}/evaluation-plan/generate`：幂等触发生成；
- `POST /api/v2/jobs/{job_id}/evaluation-plan/regenerate`：重新生成失败计划。
- `GET /api/v2/applications/{application_id}/screening`：查询当前报告与最近运行；
- `POST /api/v2/applications/{application_id}/screening`：普通幂等初筛；
- `POST /api/v2/applications/{application_id}/screening/re-evaluate`：单人重新评估；
- `POST /api/v2/jobs/{job_id}/screening/re-evaluate-batch`：同岗位 1—20 人重新评估；
- `PUT /api/v2/applications/{application_id}/current-resume`：切换当前 Resume，只标记过期而不自动重评。

页面使用方法：

- 在 `/app/jobs` 点击“评价计划”，查看生成状态和只读事项；只有开放岗位的失败计划可以重新生成。
- 在 `/app/screening` 点击“查看 AI 报告”，查看建议分、标签、综合评价、逐项理由、可展开证据、额外亮点、权衡和面试重点。
- `queued/running` 时页面每 4 秒刷新；成功、失败、暂停或关闭抽屉后停止。
- 没有报告时“开始初筛”执行普通幂等触发，可能复用相同报告或运行。
- 有报告时“重新评估”需要确认；新运行期间和失败后旧报告继续保留。
- 报告过期时继续显示 Resume、JD 或评价计划变化原因，不会自动调用 AI。
- 勾选同一开放岗位的 1—20 个 Application 后可以批量重新评估；跨岗位、超过 20 人和重复提交会在请求前阻止。

AI 分数和展示标签只解释当前简历呈现出的岗位匹配度，不是通过、备选、淘汰或录用结论。0 分表示“当前简历未体现”，不表示候选人事实上不会。当前仍是内部开发接口边界，尚未实现登录/RBAC。

## 8. AI 配置

复制环境变量模板：

```powershell
Copy-Item .env.example .env
```

需要测试简历结构化时填写：

```dotenv
DEEPSEEK_API_KEY=你的密钥
```

当前专用配置包括简历结构化的 `RESUME_STRUCTURE_*`、JD 评价计划的 `JOB_EVALUATION_PLAN_*`、候选人评价的 `SCREENING_EVALUATION_*` 和 PostgreSQL 任务工人的 `SCREENING_WORKER_*`。旧 `RUBRIC_GENERATION_*` 和 `SCREENING_MODEL_*` 已移除。`.env` 含有密钥，不能提交到 Git。

## 9. 数据与文件安全

- 正式数据库是 PostgreSQL，不使用 SQLite。
- 数据库结构由 Alembic migration 管理。
- 当前数据库版本应为 `b7f2c9d4e816`。
- 系统不会自动 seed 候选人、岗位或评分数据。
- `sample_data/` 只用于 Prompt 或测试样例，不会自动写入业务表。
- 简历保存在 `STORAGE_DIR` 指向的私有目录。
- 旧 Rubric 删除迁移的 downgrade 只能恢复空表结构，不能恢复已删除数据。

停止基础设施：

```powershell
docker compose stop postgres redis chroma
```

这不会删除 Docker volume。不要把 `docker compose down -v` 当作普通停止命令，因为 `-v` 会删除持久化数据卷。

## 10. 常见问题

### 为什么启动后没有数据？

系统不自动制造演示数据。请先创建岗位、简历和自己的开发记录。

### 为什么初筛工作台没有刚创建的 Candidate？

只有 Candidate 还不够，还要创建绑定岗位和当前 Resume 的 Application。

### 为什么看不到 AI 分数？

先在岗位页检查当前评价计划是否 `ready`，再检查 Application 是否绑定已解析 Resume。`waiting_resume` 表示简历不可用，`waiting_plan` 表示计划未就绪，`queued/running` 表示后台仍在处理。失败时页面只显示安全错误，已有旧成功报告不会被删除。

### 为什么简历 AI 结构化报配置错误？

真实结构化需要有效的 DeepSeek 配置。检查 `.env`，然后重新启动后端。

### 为什么 `/stage3/...` 打不开？

内部工作台已统一为 `/app/*`，旧施工期路由不再保留。

## 11. 尚未完成

- 正式公开投递提交；
- 真实 DeepSeek 的 20 份 JD 与 20 组 JD/Resume 专项质量验收；
- 面试、Offer 和录取闭环；
- 登录、角色和权限；
- 综合 Agent 与 RAG；
- 已验收的生产部署。

当前进度与下一小步以 [PROJECT_STATE.md](PROJECT_STATE.md) 为准；启动和验证命令见 [README.md](README.md)。
