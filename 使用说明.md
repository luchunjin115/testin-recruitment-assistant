# HR Agent 招聘提效平台使用说明

> 适用版本：2026-08-20 阶段 7 旧 Rubric 删除后的开发版
>
> 系统不附带演示数据。第一次启动后岗位、候选人、简历和 Application 列表为空，属于正常现象。

## 1. 启动项目

请先安装 Docker Desktop、Python 3.11、Node.js 18 或更高版本。在项目根目录双击：

```text
launch\start_project.bat
```

脚本会启动 PostgreSQL、Redis 和 Chroma，执行 Alembic 数据库迁移，然后启动后端与前端。默认页面是：

```text
http://localhost:5173/app/jobs
```

只检查环境：

```powershell
launch\start_project.bat -CheckOnly
```

启动但不自动打开浏览器：

```powershell
launch\start_project.bat -NoBrowser
```

后端 Swagger 接口文档位于 `http://localhost:8000/docs`。

## 2. 当前页面

| 页面 | 地址 | 当前用途 |
| --- | --- | --- |
| Dashboard | `/app/dashboard` | 查看真实岗位、候选人和待处理 Application 统计 |
| 简历管理 | `/app/resumes` | 查看已上传简历 |
| 候选人管理 | `/app/candidates` | 查看 HR 已通过的 Application 所对应候选人 |
| 新增候选人 | `/app/candidates/new` | HR 人工直通，或上传简历后识别并核对资料 |
| 岗位管理 | `/app/jobs` | 创建、编辑、开放、关闭、重新开放和安全删除岗位 |
| HR 初筛工作台 | `/app/screening` | 录入 Application，并独立执行通过、备选、淘汰和决定反转 |
| 报告中心 | `/app/reports` | 查看通用 Report；新版 AI 初筛报告尚未实现 |
| 公开投递预览 | `/apply` | 查看开放岗位和页面外观，当前不能正式提交 |

当前没有登录和权限系统，仍是开发环境工作台。

## 3. 当前推荐流程

```text
创建并开放 Job
  -> 上传 Resume、提取原文、可选 AI 结构化
  -> 创建或复用稳定 Candidate
  -> 创建绑定当前 Resume 的 Application
  -> HR 独立决定通过、备选或淘汰
  -> StageHistory 保存决定记录
```

旧 Rubric、五维权重、Python 确定性评分、推荐上限和多份 ScreeningResult 历史已经删除，不应再按旧流程准备评分标准或调用旧接口。

## 4. 创建并开放岗位

1. 打开 `/app/jobs`。
2. 新增岗位，填写岗位名称、部门、地点、职位描述和结构化要求。
3. 先保存草稿，检查完整后再开放。

草稿和关闭岗位不能接收新的申请；历史 Application 仍能显示其关闭岗位信息。已有 Candidate、Resume、Application 或 Report 引用的岗位不能直接删除。

## 5. 简历与候选人

在 `/app/candidates/new` 可以手工填写资料，也可以：

1. 上传 PDF、DOCX 或 TXT，单个文件最大 10 MB。
2. 后端把文件保存在私有目录。
3. 提取原文。
4. 调用 DeepSeek 生成结构化草稿。
5. HR 核对普通字段、教育、工作、项目和技能候选。
6. 确认后创建或复用 Candidate，并把 Resume 绑定到 Application。

AI 结构化草稿只是辅助填表，不是事实来源。联系方式、时间、公司、学校和经历在保存前都需要人工核对。

Candidate 表示“这个人”，Application 表示“这个人用哪份简历申请哪个岗位”。同一 Candidate 可以申请多个岗位，每个 Application 都独立绑定自己的当前 Resume，不能串用其他申请的简历。

## 6. Application 与 HR 决策

打开 `/app/screening`：

- “录入待审核申请”会先上传或选择已解析 Resume，再创建 Application。
- HR 可以直接通过、进入备选或淘汰，不依赖 AI 状态或 AI 分数。
- 改变已有决定时必须填写岗位相关理由。
- 淘汰后可以按明确理由撤销，回到人工审核。
- 每次决定写入 StageHistory，便于追溯。
- 年龄、性别、民族、婚育、籍贯、照片和学校名气等不能作为决定理由。

HR 人工直通入口会在一个工作流中保存 Candidate、Resume 绑定、Application 和首次阶段历史。它不会自动调用已经删除的旧评分链路。

## 7. AI 初筛当前边界

阶段 7 正在重建 JD 驱动的 AI 初筛。当前尚未实现：

- `JobEvaluationPlan`；
- 新版单份当前 AI 初筛报告；
- 重新评估、失败保留旧成功报告和运行日志；
- 新版完整报告前端。

因此当前页面只提供 Application 和 HR 决策公共能力。不要把它描述成已经完成 AI 岗位匹配评分。

## 8. AI 配置

复制环境变量模板：

```powershell
Copy-Item .env.example .env
```

需要测试简历结构化时填写：

```dotenv
DEEPSEEK_API_KEY=你的密钥
```

当前专用配置是 `RESUME_STRUCTURE_*`。旧 `RUBRIC_GENERATION_*` 和 `SCREENING_MODEL_*` 已移除；新版初筛需要的配置将在对应实现步骤增加。`.env` 含有密钥，不能提交到 Git。

## 9. 数据与文件安全

- 正式数据库是 PostgreSQL，不使用 SQLite。
- 数据库结构由 Alembic migration 管理。
- 当前数据库版本应为 `c4a9d8e7f621`。
- 系统不会自动 seed 候选人、岗位或评分数据。
- `sample_data/` 只用于 Prompt 或测试样例，不会自动写入业务表。
- 简历保存在 `STORAGE_DIR` 指向的私有目录。
- 旧 Rubric 删除迁移的 downgrade 只能恢复空表结构，不能恢复已删除数据。

停止基础设施：

```powershell
docker compose stop postgres redis chroma
```

这不会删除 Docker volume。不要把 `docker compose down -v` 当作普通停止命令，因为 `-v` 会删除持久化数据卷。

## 10. 常见问题

### 为什么启动后没有数据？

系统不自动制造演示数据。请先创建岗位、简历和自己的开发记录。

### 为什么初筛工作台没有刚创建的 Candidate？

只有 Candidate 还不够，还要创建绑定岗位和当前 Resume 的 Application。

### 为什么现在看不到 AI 分数？

旧评分体系已经按新设计删除，新版 JD 驱动报告尚未进入实现步骤。当前缺少分数是预期状态，不是接口故障。

### 为什么简历 AI 结构化报配置错误？

真实结构化需要有效的 DeepSeek 配置。检查 `.env`，然后重新启动后端。

### 为什么 `/stage3/...` 打不开？

内部工作台已统一为 `/app/*`，旧施工期路由不再保留。

## 11. 尚未完成

- 正式公开投递提交；
- JobEvaluationPlan 和新版 AI 初筛报告；
- 面试、Offer 和录取闭环；
- 登录、角色和权限；
- 综合 Agent 与 RAG；
- 已验收的生产部署。

当前进度与下一小步以 [PROJECT_STATE.md](PROJECT_STATE.md) 为准；启动和验证命令见 [README.md](README.md)。
