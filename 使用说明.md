# HR Agent 招聘提效平台使用说明

> 适用版本：2026-08-18 旧系统退役后的当前开发版
>
> 重要说明：系统不再附带旧演示数据。第一次启动后岗位、候选人、简历和初筛列表为空，属于正常现象。

## 1. 先启动项目

开始前请确保已经安装：

- Docker Desktop
- Python 3.11
- Node.js 18 或更高版本

在项目根目录双击：

```text
launch\start_project.bat
```

脚本会检查开发环境，启动 PostgreSQL、Redis 和 Chroma，执行数据库迁移，然后分别启动后端与前端。默认会打开岗位管理页：

```text
http://localhost:5173/app/jobs
```

如果只想检查环境，不启动服务：

```powershell
launch\start_project.bat -CheckOnly
```

如果想启动服务但不自动打开浏览器：

```powershell
launch\start_project.bat -NoBrowser
```

后端接口文档位于：

```text
http://localhost:8000/docs
```

这里的 Swagger 是可交互 API 文档：它会列出后端当前真实提供的接口、请求字段和响应结构，也可以直接发送开发环境请求。

## 2. 当前页面分别做什么

| 页面 | 地址 | 当前用途 |
| --- | --- | --- |
| Dashboard | `/app/dashboard` | 查看 PostgreSQL 中的真实岗位、候选人和初筛统计 |
| 简历管理 | `/app/resumes` | 查看已经上传并保存的简历 |
| 候选人管理 | `/app/candidates` | 查看候选人、进入详情和维护基础资料 |
| 新增候选人 | `/app/candidates/new` | 手工录入，或上传简历后识别并导入资料 |
| 岗位管理 | `/app/jobs` | 创建、编辑、开放、关闭、重新开放和删除岗位 |
| AI 初筛中心 | `/app/screening` | 查看 Application 队列，进行单人或小批量评分 |
| 报告中心 | `/app/reports` | 查看已经存在的 Report，当前不能从页面生成新报告 |
| 公开投递预览 | `/apply` | 查看开放岗位和投递页面外观，当前不能正式提交 |

当前没有登录和权限系统，因此它仍是开发环境工作台，不应直接当作已经可以公网交付的生产系统。

## 3. 推荐使用顺序

当前最容易验证的主流程是：

```text
创建岗位
  -> 开放岗位
  -> 新增候选人并处理简历
  -> 通过 API 准备 Rubric 和 Application
  -> 在 AI 初筛中心评分
  -> 由 HR 查看结果并作决定
```

其中岗位、候选人、简历和评分队列已有前端页面；Rubric 编辑发布、Application 完整录入、结果证据/历史与 HR 决策目前主要由后端 API 支持，前端交互还在阶段 7 中继续补齐。

## 4. 创建并开放岗位

1. 打开 `/app/jobs`。
2. 点击新增岗位。
3. 填写岗位名称、部门、工作地点、职位描述和结构化要求。
4. 先保存为草稿，检查内容后再开放。

岗位状态会影响后续流程：

- 草稿岗位不会进入新的投递和初筛选择范围。
- 开放岗位可以被候选人录入、Application 和初筛流程选择。
- 关闭岗位不能继续接收新的申请。
- 已关闭岗位可以重新开放。
- 已被业务数据引用的岗位不能随意删除，系统会执行安全校验。

## 5. 新增候选人与处理简历

打开 `/app/candidates/new`，可以选择两种方式。

### 方式 A：手工录入

直接填写候选人的姓名、手机号、邮箱和其他基础资料，然后保存。

### 方式 B：上传简历并识别

1. 上传 PDF、DOCX 或 TXT 文件，单个文件最大 10 MB。
2. 系统把文件保存到后端私有目录，不通过公开静态地址暴露。
3. 执行原文提取。
4. 调用 AI 生成结构化资料。
5. 人工核对姓名、联系方式、教育、工作和项目经历。
6. 确认后把资料导入并绑定到候选人。

“结构化”就是把简历里的自由文本整理成系统能够校验和保存的字段。AI 识别结果不是事实来源，尤其是联系方式、时间和公司名称，保存前必须由人检查。

如果中途放弃未绑定的上传流程，系统会按当前清理规则处理临时对象；已经绑定候选人的简历不会被当作临时文件误删。

简历 AI 结构化需要有效的 DeepSeek 配置。没有配置 Key 时项目仍可启动，但这一步会返回配置错误，不代表已经识别成功。

## 6. 准备 Rubric 与 Application

AI 初筛不是拿到一份简历就直接随意打分，它还需要两个前置对象：

- Rubric：某个岗位本次使用的评分标准和权重版本。
- Application：某位候选人拿当前简历申请某个岗位的独立记录。

Rubric 的模板、AI 草稿、人工修订、发布和版本控制已经在后端实现；Application 的录入、身份解析、状态、历史和 HR 决策也已经在后端实现。当前前端尚未提供它们的全部操作界面，因此开发验证时请使用 `/docs`：

1. 在 `jobs` 接口组中为目标岗位准备并发布 Rubric。
2. 在 `applications` 接口组中使用 intake 接口创建 Application。
3. 以 Swagger 当前展示的请求 Schema 为准填写字段，不要照抄旧文档中的请求体。
4. 创建完成后回到 `/app/screening` 刷新队列。

Application 必须绑定开放岗位、候选人和当前 Resume，并满足姓名、手机号、邮箱等当前合同要求。后端会明确区分“新建”“复用已有候选人”和“身份冲突”，不会静默合并可疑人员。

## 7. 使用 AI 初筛中心

打开 `/app/screening` 后，可以看到当前 Application 工作队列。

### 单人评分

选择一条满足前置条件的 Application 发起评分。系统会组合：

- Python 确定性规则，例如明确年限、学历和结构化关键词比较；
- 已发布的岗位 Rubric；
- DeepSeek 对需要理解上下文的语义项评价。

结果会保存分数、推荐、证据、输入快照和所用版本，便于后续追溯。

### 小批量评分

当前一次只能选择同一岗位的 1—5 条 Application。每一条会独立显示：

- 已完成；
- 失败；
- 被前置条件阻止；
- 复用已有结果；
- 跳过。

如果部分失败，可以只重试失败项，不需要把已经成功的项目全部重跑。

### 评分边界

- AI 不会自动通过或淘汰候选人。
- 低分、unknown、硬性条件失败或模型异常都不能直接替代 HR 决策。
- 评分失败不会伪装成成功结果。
- 评分详情、逐项证据、历史版本和完整 HR 决策前端仍是阶段 7 下一小步。

## 8. Dashboard、报告和公开投递

Dashboard 只统计数据库中的真实记录。没有数据时显示空状态或 0，不会自动补演示数字。

报告中心当前可以读取已经存在的 Report，但前端生成报告的工作流尚未开放。

`/apply` 当前只是公开投递页面预览：它能读取开放岗位，但提交按钮尚未开放。正式公开投递属于后续阶段，不能把当前页面描述为已经能接收真实求职者投递。

## 9. AI 环境配置

复制环境变量模板：

```powershell
Copy-Item .env.example .env
```

在 `.env` 中至少填写实际使用的模型 Key，例如：

```dotenv
DEEPSEEK_API_KEY=你的密钥
```

主要专用配置分为：

- `RESUME_STRUCTURE_*`：简历结构化；
- `RUBRIC_GENERATION_*`：岗位 Rubric 生成；
- `SCREENING_MODEL_*`：候选人语义评分。

`.env` 包含密钥，不能提交到 Git。`LLM_PROVIDER=mock` 是通用 LLM 默认项，不保证上述三条专用 AI 链路一定返回模拟成功结果。

## 10. 数据与文件安全

- 当前正式数据库是 PostgreSQL，不使用 SQLite。
- 数据库结构通过 Alembic migration 管理。
- 系统不会自动 seed 候选人、岗位或评分演示数据。
- `sample_data/` 是受版本控制的 Prompt/测试样例，不会自动写入业务表。
- 简历保存在 `STORAGE_DIR` 指向的私有目录。
- 当前仓库没有“恢复旧演示数据”或“重置成演示状态”的脚本。

停止基础设施请使用：

```powershell
docker compose stop postgres redis chroma
```

这只会停止容器，不会删除 Docker volume。不要把 `docker compose down -v` 当作普通停止命令，因为 `-v` 会删除持久化数据卷。

## 11. 常见问题

### 启动后为什么什么数据都没有？

因为旧演示数据已经按决定永久清理，新系统也不会自动造数据。请先在岗位页和候选人页创建自己的开发数据。

### 为什么公开投递不能提交？

当前只完成页面预览和开放岗位读取，正式提交、后台队列与自动初筛属于后续阶段。

### 为什么初筛队列里没有刚创建的候选人？

Candidate 代表“这个人”，Application 代表“这个人申请这个岗位”。只有候选人还不够，必须另外创建满足合同的 Application，并准备已发布 Rubric。

### 为什么 AI 接口报配置错误？

项目可以不带 Key 启动，但真实简历结构化、Rubric 生成和语义评分需要有效的模型配置。请检查 `.env`，并重新启动后端使配置生效。

### 旧地址 `/stage3/...` 为什么打不开？

施工期地址已经正式改为 `/app/...`，没有保留兼容路由。内部工作台使用 `/app/*`，公开页面使用 `/apply`。

## 12. 当前尚未完成

以下能力不要在演示、简历或面试中说成已经交付：

- 正式公开投递提交；
- Rubric 完整前端编辑与发布；
- 初筛详情、证据、历史和 HR 决策完整前端交互；
- 面试、Offer 和录取闭环；
- 登录、角色和权限；
- 综合 Agent 与 RAG；
- 已验收的生产部署。

当前进度与下一小步以 [PROJECT_STATE.md](PROJECT_STATE.md) 为准；启动、架构和验证命令请查看 [README.md](README.md)。
