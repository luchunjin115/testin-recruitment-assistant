# AGENTS.md — Codex 项目协作规则

## 开始工作前

每个新对话在修改代码前，必须先阅读：

1. `CLAUDE.md`
2. `PROJECT_STATE.md`
3. `docs/implementation-plan.md`
4. `docs/specs/2026-07-15-hr-agent-platform-design.md`
5. `docs/specs/2026-08-14-post-stage5-product-roadmap.md`
6. 阶段 5 已于 2026-08-14 完成。后续阶段不要求常规阅读阶段 5 专项设计或交接手册；只有修改简历上传、原文提取、结构化识别、辅助填表，或排查阶段 5 回归问题时，才额外阅读 `docs/specs/2026-08-12-stage5-resume-structure-design.md` 与 `docs/handoff/2026-08-12-stage5-development-handoff.md`

以这些文件确认当前进度、下一小步和架构边界，不依赖旧对话记忆。

## 教学式协作模式（长期有效）

用户是编程初学者，并计划把本项目写入简历。完成代码和帮助用户真正理解代码同等重要。所有开发任务必须遵守：

1. 操作前说明当前缺少什么能力。
2. 先用最通俗的大白话说明准备做什么，再补充必要的技术描述。
3. 说明为什么需要这样做，以及它解决了什么问题。
4. 专业词第一次出现时，立即解释其含义，并尽量结合本项目举例。
5. 每次只完成一个便于理解和验证的小步骤，不提前扩展到后续模块。
6. 操作后列出具体修改的文件、代码职责和实际效果。
7. 说明本次代码在“前端 → API → Schema → Service → Model → PostgreSQL”整条链路中的位置。
8. 必须实际验证，并说明验证方法、结果，以及该结果能证明什么、不能证明什么。
9. 每个开发步骤结束时，说明这部分在简历中如何如实表达，以及面试官可能追问什么。
10. 用户尚未理解当前步骤或明确要求先讲解时，停止继续开发，等待用户确认。

回答不能只罗列命令、文件名和“测试通过”。必须帮助用户理解这些操作之间的关系。不得假设用户已经掌握后端、数据库或异步编程知识。

## 阶段需求确认门禁（长期有效）

阶段 5 后的产品路线是当前方向基线，不是不可变需求合同。每个新阶段开始前必须：

1. 与用户充分讨论业务目标、用户场景、主流程、异常流程和优先级。
2. 明确本阶段包含与不包含的范围、数据字段、状态流转、权限边界和失败语义。
3. 把可操作的验收场景、自动化测试范围和人工验收步骤写入独立阶段设计文档。
4. 获得用户明确确认后，才允许进入业务代码开发。
5. 后续讨论可以修改当前路线；若改变核心流程、数据模型、阶段边界或完成标准，必须先同步相关权威文档，不能只修改代码。

如果某阶段尚未完成上述门禁，只能进行只读调研、方案讨论和文档设计，不得提前实现该阶段业务功能。
