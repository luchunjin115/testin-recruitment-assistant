# AGENTS.md — Codex 项目协作规则

## 开始工作前

每个新对话先阅读 `CLAUDE.md`、`PROJECT_STATE.md` 的当前摘要和 `docs/DOCUMENT_INDEX.md`。随后只阅读与任务直接相关的设计、代码和测试，不默认通读历史资料。

- 核心业务合同、数据模型、跨模块流程或验收标准变化：完整阅读当前阶段设计；
- 局部 Bug、测试、文案和文档整理：阅读相关章节和实际代码即可；
- 跨阶段或总体架构变化：再读取对应 planning/architecture 文档；
- `docs/archive/` 只用于明确的历史追溯。

修改前检查 `git status` 和相关差异，保护用户已有修改、未提交证据和本地数据。

## 协作方式

用户是编程初学者，并计划把项目写入简历。开发时先用通俗语言说明问题和改法；专业词第一次出现时简要解释。较大改动说明其在“前端 → API → Schema → Service → Model → PostgreSQL”中的位置。

修改后列出主要文件、职责和效果，并实际验证，说明结果能证明什么、不能证明什么和剩余风险。不要求为机械修改重复完整教学模板；用户要求先讲解或关键业务选择尚未确认时，停止实现并等待确认。

## 变更门禁

以下高影响变化必须先更新设计并获得用户确认：

- 新阶段、核心用户流程、阶段范围或完成标准；
- API/Schema/数据库合同、状态、权限或失败语义；
- migration、AI 评分原则、隐私边界或 HR 决策权；
- 阶段 7 以外的新付费服务或明显扩大预算的调用、破坏性数据操作或不可恢复的证据写入。

局部 Bug、现有合同内的 Prompt 改进、测试修正、质量工具维护、文档整理和普通重构，不要求先追加长篇实施批次。用简短计划说明范围后可直接完成相关修改和验证；若发现必须改变高影响合同，再停止确认。

默认完成一组紧密相关的修改，不强制每个红灯单独停一轮，也不为满足流程创建新的编号、设计章节、状态文件、运行器或测试文件。不得靠删除断言、降低质量门槛、增加样本特例或隐藏失败换取通过。

## 阶段 7 质量迭代

- 当前问题统一维护在一个问题清单；
- 优先复用现有质量合同、fixture、运行器和测试；
- 先区分模型、Prompt、Schema/Service、验收工具和标签责任，再决定修改层；
- Service 只负责确定性的结构、引用、安全和事务规则，不用关键词裁判普通语义；
- 不为单个正式 case 写生产特例；只有新的真实付费结果确实需要隔离时，才新建 run 和 write-once 证据；
- 阶段 7 使用项目既有 DeepSeek 配置进行真实质量测试属于用户长期授权，无需逐次确认调用次数、价格或是否开始；
- 不为阶段 7 DeepSeek 调用单独创建价格快照、authorization JSON、授权批次或等待门禁；模型、attempt、token 和估算费用直接记录在同一份结果中；
- 单次逻辑质量运行沿用 USD 2 自动硬上限，内容错误不自动重试，基础设施重试必须有明确上限；预计或实际触及上限时自动停止并报告，不把再次确认设置成阶段 7 的常规门禁；
- 中间测试只记录关键方向和结论，不向多份权威文档重复追加命令与数字；
- 阶段完成时只保留最终合同、主线总结、最终证据和保护当前产品的测试。

## 文档与验证

- `PROJECT_STATE.md` 只记录当前阶段、结论、风险和下一步；
- 当前设计只记录现行业务合同、关键取舍和验收标准；
- 问题清单记录未关闭问题，完成报告记录一条主线和最终验收，详细过程由 Git 历史恢复；
- 同一状态或测试结论只维护一次，过期停止点不得与当前入口并列；
- 验证强度与风险匹配：先跑直接相关测试，再按影响扩大到后端、数据库、前端或浏览器；
- 阶段 7 DeepSeek 调用按上述长期授权执行；其他真实付费调用先说明模型、次数、价格、费用上限和写入位置并取得授权；
- 不输出或提交 Key、`.env`、真实个人信息或未脱敏简历；
- 删除或移动文件前确认精确路径、引用和 Git 恢复点。
